{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "GenericCase",
  "description": "C.C.가 지정되지 않거나 목록에 없는 환자의 키워드 카드를 정규화한 범용 JSON. AI Stage 1 출력 형식.",
  "type": "object",
  "required": [
    "inputs",
    "past_history",
    "medication_history",
    "operation_history",
    "family_history"
  ],
  "properties": {
    "inputs": {
      "type": "array",
      "description": "의사가 입력한 키워드 카드 목록. 각 카드는 raw_text, normalized_text, category, sections를 포함.",
      "items": {
        "type": "object",
        "required": ["raw_text", "normalized_text", "category", "sections"],
        "properties": {
          "raw_text": {
            "type": "string",
            "description": "의사가 입력한 원본 텍스트. 절대 수정 금지."
          },
          "normalized_text": {
            "type": "string",
            "description": "AI가 표준 의학 용어로 정규화한 텍스트."
          },
          "category": {
            "type": "string",
            "description": "카드의 임상 분류. 아래 enum 값 중 가장 적합한 것 하나를 선택.",
            "enum": [
              "onset",
              "duration",
              "location",
              "character",
              "severity",
              "aggravating_relieving_factor",
              "associated_symptom",
              "prior_episode",
              "vital_sign",
              "past_history",
              "medication_history",
              "operation_history",
              "family_history",
              "npo_status",
              "other"
            ]
          },
          "sections": {
            "type": "array",
            "description": "이 카드가 사용될 섹션 목록. 복수 선택 가능. 카드-섹션 매핑 기준: onset/duration/location/character/severity/aggravating_relieving_factor → [hpi] | associated_symptom/prior_episode/vital_sign → [hpi] | past_history/medication_history/operation_history/family_history → [history] 전용 (HPI에 포함 금지)",
            "items": {
              "type": "string",
              "enum": ["pi", "template", "history", "pe"]
            },
            "minItems": 1
          }
        }
      }
    },
    "onset": {
      "type": ["string", "null"],
      "description": "증상 발생 시점 (예: '3일 전', '금일 오전 10시경'). 없으면 null."
    },
    "onset_character": {
      "type": ["string", "null"],
      "description": "발생 양상 — 'acute'(갑작스러운) 또는 'gradual'(서서히 악화). 없으면 null."
    },
    "duration": {
      "type": ["string", "null"],
      "description": "증상 지속 시간 (예: '30분', '간헐적', '지속적'). 없으면 null."
    },
    "location": {
      "type": ["string", "null"],
      "description": "증상 발생 부위. 없으면 null."
    },
    "character": {
      "type": ["string", "null"],
      "description": "증상 양상/성질 (예: 'sharp', 'dull', 'burning', 'pressure'). 없으면 null."
    },
    "severity": {
      "type": ["string", "null"],
      "description": "증상 강도 표현 (예: 'NRS 7/10', 'severe', 'mild'). 없으면 null."
    },
    "aggravating_factor": {
      "type": ["string", "null"],
      "description": "증상 악화 요인. 없으면 null."
    },
    "relieving_factor": {
      "type": ["string", "null"],
      "description": "증상 완화 요인. 없으면 null."
    },
    "ongoing": {
      "type": ["boolean", "null"],
      "description": "내원 시점에도 증상이 지속 중인지 여부. true/false/null."
    },
    "associated_symptoms": {
      "type": ["array", "null"],
      "description": "동반 증상 목록. 없으면 null.",
      "items": {
        "type": "string"
      }
    },
    "prior_episode": {
      "type": ["string", "null"],
      "description": "유사 증상 과거 에피소드. 없으면 null."
    },
    "past_history": {
      "type": ["array", "null"],
      "description": "과거력 목록. History 출력 시 항상 포함; 내용 없으면 null → (-)로 출력.",
      "items": {
        "type": "string"
      }
    },
    "medication_history": {
      "type": ["array", "null"],
      "description": "복용 약물 목록. History 출력 시 항상 포함; 내용 없으면 null → (-)로 출력.",
      "items": {
        "type": "string"
      }
    },
    "operation_history": {
      "type": ["array", "null"],
      "description": "수술력 목록. History 출력 시 항상 포함; 내용 없으면 null → (-)로 출력.",
      "items": {
        "type": "string"
      }
    },
    "family_history": {
      "type": ["array", "null"],
      "description": "가족력 목록. null이면 History 출력 시 해당 항목 전체 생략.",
      "items": {
        "type": "string"
      }
    },
    "npo_solid": {
      "type": ["string", "null"],
      "description": "마지막 고형식 섭취 시점 (MM.DD HH:mm 형식). '|' 뒤에 추가 정보 포함 가능. 없으면 null."
    },
    "npo_liquid": {
      "type": ["string", "null"],
      "description": "마지막 수분 섭취 시점 (MM.DD HH:mm 형식). '|' 뒤에 추가 정보 포함 가능. 없으면 null."
    }
  }
}
