// 설정 페이지 — TODO: Task 008에서 실제 UI 구현
// 입력 화면 레이아웃 선택: 단독 / 상하 분할 / 좌우 분할 (갤럭시 폴드·태블릿)

export default function SettingsPage() {
  return (
    <div className="p-8">
      <h1 className="text-xl font-semibold">설정</h1>
      <p className="mt-2 text-muted-foreground">
        TODO: Task 008에서 구현 예정 (입력 화면 레이아웃 선택)
      </p>
    </div>
  );
}
