/**
 * referenceExamples(few-shot 예시)를 프롬프트에 주입할 때 공통으로 덧붙이는 안전 가드 문구.
 *
 * 4개 generate-*.ts(HPI/상용구/History/P/E)에서 일관된 원칙을 AI에게 전달하기 위해
 * 중앙집중 상수로 관리한다. 문구를 수정할 때는 이 파일 한 곳만 고치면 된다.
 *
 * 핵심 원칙: 예시의 **스타일**(문체·어휘·약어·기호·줄바꿈)만 학습하고,
 * 예시 안의 **내용**(수치·증상·병력·시간)은 현재 차팅에 절대 복사하지 않는다.
 */
export const FEW_SHOT_GUARD = `아래 referenceExamples 배열은 이 항목의 **모범 출력 예시**입니다.

다음 원칙을 반드시 지켜 주세요.

1. 예시의 **스타일만 학습**합니다. 구체적으로는 문체, 어휘 선택, 의학 약어 표기, 기호 사용(+/-, ->, ± 등), 줄바꿈 구조, 섹션 순서를 모방합니다.
2. 예시 안의 **구체적 사실**(수치, 증상, 병력, 과거력, 시간 표현, 진단명 등)은 **절대 현재 차팅에 끌어오지 마세요.** 예시는 다른 환자의 기록입니다.
3. 현재 차팅의 모든 내용은 **오직 현재 입력(raw_text 카드·structured JSON)에서만** 나와야 합니다.
4. 입력에 없는 정보는 예시를 모방해서라도 **추가하지 마세요.** 누락 정보는 상용구 기본값('-', '(-)', 공란) 규칙을 따릅니다.
5. 예시가 여러 개 제공되면 각각의 스타일을 비교해 공통된 포맷 규칙을 뽑아 적용하고, 특정 예시의 고유한 수치/병력을 그대로 가져오는 일이 없게 합니다.`;
